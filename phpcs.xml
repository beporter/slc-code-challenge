<?xml version="1.0"?>
<ruleset name="BP DiffBot Products">
	<description>Wrapper for the WordPress Coding Standard.</description>

	<file>./</file>
	<exclude-pattern>/includes/class-diffbot.php</exclude-pattern>
	<exclude-pattern>/wpcs/*</exclude-pattern>
	<exclude-pattern>/wp-cli/*</exclude-pattern>
	<exclude-pattern>*/index.php</exclude-pattern>

	<arg name="report" value="full"/>

	<!-- Include standard rules as a base. -->
	<rule ref="wpcs/WordPress/ruleset.xml" />
</ruleset>
